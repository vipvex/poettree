<%= form_with(model: poem, local: true) do |form| %>
  <% if poem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poem.errors.count, "error") %> prohibited this poem from being saved:</h2>

      <ul>
      <% poem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :poem_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :poem_body %>
  </div>

  <%= form.hidden_field :user_id, id: :poem_user_id, value: current_user.id %>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url, id: :poem_image_url %>
  </div>

  <div class="field">
    <%= form.label :add_to_poet_tree %>
    <%= form.check_box :shared, id: :poem_shared %>
  </div>

  <div class="actions">
    <%= form.submit 'Save' %>
  </div>
<% end %>
