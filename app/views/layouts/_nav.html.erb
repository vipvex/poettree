<nav class="ctr-margin logged-out-nav">
    <%= link_to root_url do %>
      <p class="logo">iPoetTree</p>
    <% end %>
    <a href="#" class="donate">Donate</a>
    <%= link_to "About Us", about_us_url %>
    <% if current_user %>
      <%= link_to 'Dashboard', dashboard_path %>
    <% else %>
      <a href="#" data="login-modal-ctr">Log In</a>
    <% end %>
    <a href="#" class="primary-red" data="login-modal-ctr">Sign Up</a>
    <div id="hamburger">
      <span></span>
      <span></span>
      <span></span>
      <span></span>
    </div>
</nav>
<div class="ctr-margin mobile-nav-box hidden">
 <a href="#" class="donate">Donate</a>
  <%= link_to "About Us", about_us_url %>
  <% if current_user %>
    <%= link_to 'Dashboard', dashboard_path %>
  <% else %>
    <a href="#" data="login-modal-ctr">Log In</a>
  <% end %>
  <a href="#" class="primary-red" data="login-modal-ctr">Sign Up</a>
</div>